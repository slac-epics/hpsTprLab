#==============================================================
#
#  Name: RELEASE_NOTES
#
#  Rem:  This files describes release notes for
#        the XXX IOC Application.
#
#  Auth: xx-Dec-2012, XXX         (xxx)
#  Rev:  dd-mmm-yyyy, Reviewer's Name    (USERNAME)
#--------------------------------------------------------------
#
R1.1.2     Nov 11, 2021   Justin Chen (justinc)
           - build with tprTrigger R2.5.5, updated configure/RELEASE.local

R1.1.1     Nov 10, 2021   Justin Chen (justinc)
           - build with tprTrigger R2.5.4


R1.1.0     Sep 14, 2021
           IMPORTANT: please, do not try this version before upgrading TPG to R1.13.0 or later
           - using descedning order divisors (TPG R1.13.0 or later)
           - apply UED template for sioc-b84-ts61, UED test stand
           - build with tprTrigger R2.5.1
           

R1.0.2     Sep 10, 2021
           - build against yamlLoader R2.2.0, for overriding device file name for multiple PCIe TPR instances
             ex) ioc-b84-ts61,    multiple PCIe TPR instances and device file overriding
                 ioc-diag0-ts01,  single PCIe TPR instances, no overriding for the deivce file
           - build against
                 tprTrigger R2.4.0
                 tprPattern R2.2.0
                 ATCACommon R1.3.0-BR-R5

R1.0.1     Sep 9, 2021   Kukhee Kim (khkim)
           - clean up yaml directories and move yaml director into $(TOP)/firmware/$(hash)/yaml
           - add 001TopLevel.yaml file for slot_b PCIe TPR
             -------------------------------------------------------
             Top level yaml       PCIe TPR         device file path
             -------------------------------------------------------
             000TopLevel.yaml     1st instance      /dev/tpra
             001TopLevel.yaml     2nd instance      /dev/tprb
             -------------------------------------------------------
           (remarks) need to improve cpsw/framework to allow overrride 000Toplevel.yaml for the PCIe TPR device file name

R1.0.0     Sep 8, 2021   Kukhee Kim (khkim)
           - use PCIe TPR firmware d381d3e

           - prepare initial release (support both PCIe TPR and ATCA Timing Core)
           - support firmware (PCIe TPR) d381d3e

           PCIe TPR test iocs
           - example for 2 PCIe TPRs in a single ioc
             iocBoot/sioc-pcie-tpr

           - example for magnet ioc (1 PCIe TPR in a single ioc) 
             iocBoot/sioc-diag0-ts01
             iocBoot/sioc-sph-ts01
             iocBoot/sioc-sps-ts01

             
